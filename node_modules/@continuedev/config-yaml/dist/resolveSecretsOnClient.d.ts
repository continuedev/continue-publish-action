import { ClientConfigYaml } from "./schemas/index.js";
type SecretProvider = (secretNames: string[]) => Promise<{
    [key: string]: string;
}>;
/**
 * Take a ConfigYaml with apiKeySecrets, and look to fill in these secrets
 * with whatever secret store exists in the client.
 */
export declare function resolveSecretsOnClient(configYaml: ClientConfigYaml, getSecretsFromClientStore: SecretProvider, getSecretsFromServer: SecretProvider): Promise<ClientConfigYaml>;
export {};
